// tslint:disable:no-magic-numbers

import { Base, to } from '../../../../src'

const SQRT_THREE: Base = to.Base(Math.sqrt(3))
const SQRT_TWO: Base = to.Base(Math.sqrt(2))

export {
    SQRT_THREE,
    SQRT_TWO,
}
