import { to, Translation } from '@musical-patterns/utilities'

const TRANSLATION_TO_SPECIALIZE_COORDINATES_FOR_THEME: Translation = to.Translation(1)

export {
    TRANSLATION_TO_SPECIALIZE_COORDINATES_FOR_THEME,
}
